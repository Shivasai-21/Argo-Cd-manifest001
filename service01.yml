apiVersion: v1
kind: Service
metadata:
  name: my-helm-app
  labels:
    app: my-helm-app
spec:
  type: LoadBalancer   # or NodePort/ClusterIP depending on your needs
  selector:
    app: my-helm-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
